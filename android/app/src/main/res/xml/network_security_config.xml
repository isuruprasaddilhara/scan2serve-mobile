<?xml version="1.0" encoding="utf-8"?>
<!-- Allow HTTP to local dev servers (Django on LAN / emulator). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
